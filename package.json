{
  "name": "deepclean-aep",
  "version": "2.0.0",
  "description": "Production-ready Adobe After Effects CEP extension — dependency graph analysis and project cleanup.",
  "private": true,
  "scripts": {
    "build":       "node build.js",
    "install:dev": "node scripts/dev-install.js",
    "package":     "node scripts/package.js"
  },
  "devDependencies": {
    "esbuild": "^0.20.0"
  },
  "engines": {
    "node": ">=14.0.0"
  },
  "license": "MIT"
}
